// the 3rd primitive type is called boolean and it represent the classic 0 or 1
// it is used mostly as a decision making variable
// so the 0 is called false
// and the 1 is called true

iAmAFemale = false
iAmAMale = true

// the alert command, although we are supposed to use it only with strings
// JavaScript will automatically convert any value that we use to string
alert(iAmAFemale) // here i have a boolean false, it is not a string
// and yet, JavaScript will convert it to a string
// false => "false"
// and only then present it





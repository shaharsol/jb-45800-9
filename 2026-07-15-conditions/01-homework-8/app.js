width = +prompt('enter the width of the box')
height = +prompt('enter the height of the box')
depth = +prompt('enter the depth of the box')

volume = width * height * depth

alert(`the box volume of ${width}*${height}*${depth} is: ${volume}`)

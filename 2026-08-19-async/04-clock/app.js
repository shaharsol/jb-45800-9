/*
similar to setTimeout we also have setInterval.
setInterval invokes the callback EVERY x ms
lets say i want to do something every ten minutes:
setInterval(() => {}, 10 * 60 * 1000)

using this new information (new shit has come to light) draw a clock on the page

const now = new Date() // <- this gives you a date object containing the current time

*/